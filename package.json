{
  "name": "aaranya-built-homes",
  "version": "1.0.0",
  "private": true,
  "description": "Boutique real-estate listing website with a single-owner dashboard",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
